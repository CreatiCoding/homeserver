server {
    listen 80;
    server_name _;   # 모든 요청 수락 (또는 example.com 으로 변경 가능)

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    access_log /var/log/nginx/hello.access.log;
    error_log /var/log/nginx/hello.error.log;
}
