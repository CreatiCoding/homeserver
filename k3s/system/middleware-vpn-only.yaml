apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: vpn-only
  namespace: live # ingress가 있는 네임스페이스에 맞추세요
spec:
  ipAllowList:
    sourceRange:
      - 10.100.0.0/24 # VPN 클라이언트 대역
      - 10.42.0.0/16 # k3s Pod CIDR (핵심)
